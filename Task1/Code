#include <stdio.h>
int main()
{
  int x,y;
  printf("Enter the value of X: ");
  scanf("%d", &x);
  if(x<5)
  y=1;
  else if(x%5==0)
  y=x/5;
  else
  y=(x/5)+1;
  printf("Minimum number of steps required are %d\n",y);
  return 0;
}

